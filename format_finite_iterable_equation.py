#finite iteratable


def fump(xi):
    for xi in range(desired_pair_number):
            print(f[i],'p',-i-xi)
    

alist='yes no maybe so how do cow now'

f=alist.split()
desired_pair_number=2
for i in range(len(f)):
    fump(len(f))
#i want it to iterate this format p[i]

# for i in range(len(p)):
    # r=p[i-2],p[i-1],p[i]